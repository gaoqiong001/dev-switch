# Dev Switch v0.1.0

> The first public release of **Dev Switch** — a Tauri-based desktop app that detects and displays your local development environment in one click: system info, 19 programming languages, 50+ development tools and network details, with per-OS install/uninstall guides, all cached in SQLite for instant subsequent launches.

**[中文版 →](v0.1.0-zh.md)**

---

## Highlights: What You Can Do Now

- **One-click full inventory** — detect 19 languages and 50+ tools in a single scan; installed items are sorted to the front
- **Clean version output** — raw `--version` banners are cleaned to the version token (e.g. `curl 8.21.0 (Windows) …` → `8.21.0`)
- **Per-OS install/uninstall guides** for every item (Windows / macOS / Linux)
- **System & network info** — OS, hostname, CPU, memory, plus local IP and network interfaces
- **Silent auto-update** — built-in updater downloads and installs updates automatically (toggleable); falls back to GitHub Release page when disabled
- **No console popups** on Windows (`CREATE_NO_WINDOW`)
- **Search, compact mode, i18n** (English / 中文) and light / dark / system themes
- **Export / import** — detection results as JSON/CSV, settings import
- **GitHub Actions CI/Release** — automated build, test, and release pipeline (push `v*` tag → build → publish to GitHub Releases)

---

## Overview

Dev Switch is a local development-environment detection app. It scans the machine for installed runtimes and tools, reports clean version numbers and install paths, and opens the matching install/uninstall guide per platform. Detection results are cached in a bundled SQLite database, so subsequent launches are instant until you hit **Refresh**.

**Release date**: 2026-08-10

**Change size**: 7 commits | 70 files changed | +13,417 / -17 lines (since the initial commit)

**Schema**: First release — no upgrade from a previous version.

---

## Features

### Detection

- **System** — OS version, hostname, CPU cores, total memory
- **Languages** — 19 programming languages (installed first)
- **Tools** — 50+ development tools & editors (installed first)
- **Network** — local IP and network interfaces

### Details & Guides

- **Version / Path / Status** per item, with cleaned version tokens
- **Install & uninstall guides** per operating system (Windows / macOS / Linux)

### UX

- **Search** — filter languages and tools by name
- **Status sorting** — installed items first; uninstalled items use a dashed border with reduced opacity
- **Compact mode** — tighter spacing to show more content

### Data & Settings

- **SQLite cache** — results persist across launches; **Refresh** clears the cache and re-scans
- **Export / Import** — export detection results as JSON or CSV; import settings back
- **Silent auto-update** — built-in updater delivers new versions automatically; toggleable in **Settings → Startup → Auto-download and install updates**; falls back to opening the GitHub Release page when disabled
- **CI/Release** — GitHub Actions automated pipeline: `ci.yml` (quality gate on push/PR), `release.yml` (build + publish on `v*` tag)

---

## Supported Platforms

- **Windows** (primary target)
- **macOS / Linux** — supported in the backend code; installers are planned for upcoming releases

---

## Upgrade Notes

- **No upgrade path** — this is the first release; there is no previous version to upgrade from, and no database migration (schema is created fresh on first launch).
- Updates from this version onward are delivered via the built-in silent updater; new versions are downloaded and installed automatically (toggleable in **Settings → Startup → Auto-download and install updates**).

---

## Download & Install

Grab the installer for your system from the [Releases](https://github.com/gaoqiong001/dev-switch/releases) page.

Windows:

- `.exe` (NSIS installer)
- `.msi` (MSI installer)

> No console windows pop up during detection on Windows.

---

## License

MIT © Gao Qiong
